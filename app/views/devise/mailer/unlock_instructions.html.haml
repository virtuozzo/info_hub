%p="#{t('devise.mailer.hello')}, #{@resource.fullname}"
%p=t('devise.mailer.unlock_instructions.body')
%p=link_to t('devise.mailer.unlock_instructions.link'), unlock_url(@resource, :unlock_token => @resource.unlock_token, :host => OnApp.configuration.system_host)
